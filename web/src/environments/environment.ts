// Local dev points at the .NET API. For the deployed build, replace apiUrl
// with the public API URL (e.g. https://your-api.onrender.com/api).
export const environment = {
  apiUrl: 'http://localhost:5099/api',
};

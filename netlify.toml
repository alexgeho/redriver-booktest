# Netlify config for the Angular frontend (in /web).
[build]
  base = "web"
  command = "npm ci && npm run build"
  publish = "dist/web/browser"

# SPA fallback so Angular client-side routes resolve on refresh.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
